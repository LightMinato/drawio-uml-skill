# 验证记录

验证日期：2026-09-18。环境：macOS，draw.io Desktop 31.4.5，Python 3。

## 已运行

- 22 项标准库 unittest 全部通过，覆盖：空 SVG、缺失连线、闭合菱形、相对路径、曲线采样、变换、压缩与裸 XML、包装元数据、共享端点交叉、穿框、节点重叠、边界接触、非二元判断、语义差异、错误退出码、导出覆盖保护及 UML 箭头样式。
- skill-creator 的 `quick_validate.py` 通过。
- draw.io CLI 实际导出三个 `.drawio` 示例为 SVG 和 PNG，逐张查看 PNG。
- `compare_semantics.py` 确认类图整理前后内容、关系类型、连接端点和多重性标签未改变。

| 样例 | 图形 / 连线 | 交叉 | 重合连线 | 穿框 | 节点重叠 | 几何结果 |
| --- | --- | --- | --- | --- | --- | --- |
| class-before | 4 / 3 | 1 | 1 | 1 | 0 | FAIL |
| class-after | 4 / 3 | 0 | 0 | 0 | 0 | CHECKS_PASSED |
| sequence | 3 / 7 | 0 | 0 | 0 | 0 | CHECKS_PASSED |

对应 JSON 报告保存在 `examples/*.audit.json`。时序图的消息箭头与生命线之间出现接近提示，视觉检查确认是正常的消息终点，未因此移动消息顺序或生命线。

## 验证边界

这验证了脚本、格式和两个 UML 图型的真实导出，跨模型技能行为评测不在本次验证范围内。活动、状态、用例、组件、部署图有设计指导，但尚未逐类做端到端渲染回归。Windows/Linux 导出路径尚未在对应系统实测。

检查器不验证完整 UML 规范，也不自动测量 HTML 标签边界或判断排版美感。曲线采样是近似值，复杂原生形状可能返回 INCOMPLETE。语义比较排除几何，因此时序图的垂直消息顺序等仍需人工或模型结合预览复核。

验证结果适用于所列样例与环境。其他输入应结合几何报告及渲染预览复核。
